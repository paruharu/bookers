<html>
  <p id = "notice"></p>
  <h1>Books</h1>

  <table>
   <tread>
     <tr>
       <th>Title</th>
       <th>Body</th>
       <th colspan = "3"></th>
     </tr>
   </tread>

   <tbody>
    <% @books.each do |list| %>

     <td><%= book.title %></td>
     <td><%= book.body %></td>
     <td>
      <a href = show_path>Show</a>
     </td>
     <td>
      <a href = edit_path>Edit</a>
     </td>
     <td>
      <a href = destroy_path>Destroy</a>
     </td>
     <% end %>
    </tbody>


  </table>


  <h2>New Book</h2>

  <!-- form_with部分 -->
　<%= form_with model:@book, local:true do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

  <% end %>

</html>